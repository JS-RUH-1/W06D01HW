import {width as t_width, height as t_height} from './shape.js';

let t_area = 1/2 * t_width * t_height;
//console.log(`Triangle width: ${t_width}, height: ${t_height} and area: ${t_area}`)

export {t_width, t_height, t_area}