import {t_width, t_height, t_area} from './triangle.js'
import {s_width, s_area} from './square.js'

console.log(`Triangle width: ${t_width}, height: ${t_height} and area: ${t_area}`)
console.log(`Square width: ${s_width}, height: ${s_width} and area: ${s_area}`)
